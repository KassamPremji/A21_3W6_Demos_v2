<<<<<<< Updated upstream
﻿@model IEnumerable<CrazyBooks_Models.Models.Book>
@{
=======
﻿@{
>>>>>>> Stashed changes
  ViewData["Title"] = "Home Page";
}
@model CrazyBooks_Models.ViewModels.HomeVM

<<<<<<< Updated upstream
<div class="text-center">
  <h1 class="display-4">Welcome everybody!</h1>
</div>

<div class="container p-3 DivBgForm">
  <div class="row pt-4">
    <div class="col-10">
      <h2 class="text-primary">Books</h2>
    </div>

  </div>
  <br /><br />

  @if (Model.Count() > 0)
  {
    <table class="table table-bordered table-striped border-primary" style="width:100%">
      <thead class="bg-primary">
        <tr>
          <th>Title</th>
          <th>ISBN</th>
          <th>Price</th>
          <th>Subject</th>
          <th>Publisher</th>
          <th></th>
        </tr>
      </thead>
      <tbody class="table-striped table-light border-dark">
        @foreach (var item in Model.OrderBy(i => i.Title))
        {
          <tr class="text-dark">
            <td width="25%">@item.Title</td>
            <td width="15%">@item.ISBN</td>
            <td width="10%">@item.Price</td>
            <td width="13%">@item.Subject.Name</td>
            <td width="25%">@item.Publisher.Name</td>
           
            <td width="20%" class="text-center">
              <div class="w-75 btn-group" role="group">
                <a class="btn btn-warning mx-2" asp-action="Details" asp-route-Id="@item.Id">
                  Details
                </a>
              </div>
            </td>
          </tr>
        }

      </tbody>
    </table>
  }
  else
  {
    <p>No book exists...</p>}
=======
<div class="container">
  <div class="text-center">
    <button class="btn btn-dark filter-button" data-filter="all">All</button>
    @foreach (var obj in Model.Subjects)
    {
      <button class="btn btn-outline-secondary filter-button" data-filter="@obj.Name.Replace(' ','_')">
        @obj.Name
      </button>}
  </div>
  <br />
  <div class="row">
    @foreach (var book in Model.Books)
    {
      <partial name="_BookCard" model="book" />// Affiche tous les Books
    }
  </div>
>>>>>>> Stashed changes
</div>

@section Scripts{
  <script>
  $(document).ready(function () {

      $(".filter-button").click(function () {

        var value = $(this).attr('data-filter');
        if (value == "all") {
          $('.filter').show('1000');
        }
        else {
          $(".filter").not('.' + value).hide('3000');
          $('.filter').filter('.' + value).show('3000');

        }
      });

    });</script>
}